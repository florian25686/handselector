import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <h1>{{title}}</h1>
    <div>
    </div>
    `
})

export class AppComponent {
	title = 'Spiegeln';
}

